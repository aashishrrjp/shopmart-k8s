# shopmart-k8s/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopmart-app-deployment
  namespace: shopmart-prod
  labels:
    app: shopmart-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: shopmart-app
  template:
    metadata:
      labels:
        app: shopmart-app
    spec:
      containers:
        - name: mart-app-container
          image: us-central1-docker.pkg.dev/mando-service-project-1/shopmart-repo/mart-app:179ed7cc01145b4abf72eef7eeae6ec2d3e117f1
          ports:
            - containerPort: 80
